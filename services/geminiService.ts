/**
 * @deprecated
 * Este módulo foi substituído por `@/lib/ai/actionsClient`.
 *
 * Mantido temporariamente como *re-export* para evitar que imports antigos quebrem
 * durante a migração. Novos códigos devem importar direto de `@/lib/ai/actionsClient`.
 */

export * from '@/lib/ai/actionsClient';
